:navigation-title: After

===================================
A page the tree writes after a fold
===================================

Last in this section's ``toctree`` and drawn before the group all the same: a
page is a row of the rail and a group is a heading with rows under it, so the
folds go last however the tree ordered them. Without this page the rule cannot
be seen — the tree already put the only group at the end.
