:navigation-title: After

===================================
A page the tree writes after a fold
===================================

Last in this section's ``toctree``, and drawn before the group all the
same. A page is a row of the rail, and a group is a heading with rows under
it. So the folds go last, whatever the tree's order. This page makes the
rule visible. The tree already put the only group at the end.
