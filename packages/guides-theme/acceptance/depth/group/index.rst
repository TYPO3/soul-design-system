:navigation-title: Group

==========================
A page with pages under it
==========================

The rail folds this one: a page that has a tree below it becomes a group, and
its own link is the first item inside the fold rather than the summary.

.. toctree::
   :maxdepth: 2

   far
