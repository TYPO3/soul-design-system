# A toolchain image reads only the manifests and its entrypoint. Source is
# mounted at runtime, so it is neither a build input nor a cache invalidator.
**
!package.json
!package-lock.json
!packages/
!packages/frontend/
!packages/frontend/package.json
!.infra/
!.infra/docker-entrypoint.sh
