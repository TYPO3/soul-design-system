<?xml version="1.0" encoding="UTF-8" ?>
<!--
    The documentation, as it is published.

    `docs/` holds two kinds of file. The `.rst` pages are documents: the
    manual, the landing page and the guidelines, rendered into the publish
    root. The `.md` files beside them are not parsed at all — `input-format`
    is `rst` — and that is what makes them useful: a prompt is handed over
    whole, so it is kept as the file it is handed over as, and the page that
    teaches it includes that same file rather than a copy of its text.

    `links_are_relative` because the site is served from a root locally and
    from a repository path on Pages, and absolute links only work at one of
    them.
-->
<guides xmlns="https://www.phpdoc.org/guides"
        input-format="rst"
        links_are_relative="true"
        theme="soul"
        default_code_language="text">
    <!-- The copyright is the holder `LICENSE` names and nobody else: a site
         and the file it ships under, disagreeing about whose the work is, is a
         question for a reader rather than an answer. -->
    <project title="Soul Design System" version="0.1.4" copyright="© 2026 The TYPO3 Project"/>
    <!-- What the bar says and shows. Both optional; without them it carries
         the project title above. -->
    <extension class="TYPO3\Soul\GuidesTheme\DependencyInjection\SoulExtension">
        <!-- The bar draws its mark at 24, so the file named here is the 24
             drawing. The optical sizes are separate files and are named per
             slot below: with no <favicon> the signet is what the theme links,
             and the tab would get the 24 mark squeezed into 16. -->
        <signet>_images/signet.svg</signet>
        <favicon href="_images/signet-s.svg" sizes="16x16"/>
        <favicon href="_images/signet-l.svg" sizes="32x32"/>
        <brand>TYPO3</brand>
        <product>Soul Design System</product>
        <navigation>
            <link href="/design-system/index"/>
            <link href="/guides-theme/index"/>
            <link href="/frontend/index"/>
        </navigation>
        <!-- No groups: the columns are the toctree, section by section, and a
             page added below one of them is in the footer the moment it is
             written. What is configured here is only what the tree cannot
             know. -->
        <footer>
            <social href="https://github.com/TYPO3/soul-design-system" label="GitHub"/>
            <!-- What this is, said plainly, and then the one proof of it a
                 reader is already standing on. Whose it is stays with the
                 copyright above, which is the line that carries a holder. -->
            <note>A shared visual language for TYPO3 community projects: product pages, documentation and interface code from one set of decisions. This site is rendered with it.</note>
        </footer>
    </extension>
</guides>
